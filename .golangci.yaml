output:
  show-stats: true
linters-settings:
  gci:
    sections:
      - standard
      - default
      - blank
      - prefix(github.com/pubgolf/pubgolf)
  wsl:
    # allow-assign-and-anything: true
    allow-cuddle-declarations: true
    force-err-cuddling: true
  exhaustive:
    check:
      - switch
      - map
linters:
  enable-all: true
  disable:
    # TODO: enable...finding a lot of duplication in rpc GetSchedule tests
    - dupl

    # No plans to enable (mostly complexity measures or extremely dogmatic).
    - cyclop
    - depguard
    - dogsled
    - execinquery
    - exhaustruct
    - funlen
    - gochecknoglobals
    - gochecknoinits
    - gocognit
    - goconst
    - godox
    - gomnd
    - lll
    - maintidx
    - prealloc
    - rowserrcheck
    - tagalign
    - tagliatelle
    - testpackage
    - unparam
    - unused
    - varnamelen

    # Deprecated checks: deactivating to suppress warning.
    - deadcode
    - exhaustivestruct
    - golint
    - ifshort
    - interfacer
    - maligned
    - nosnakecase
    - scopelint
    - structcheck
    - varcheck
